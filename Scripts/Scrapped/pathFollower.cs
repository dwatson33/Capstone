using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pathFollower : MonoBehaviour {

	//create array to hold all path nodes
	//Node[] PathNode;
	/*
	// Use this for initialization
	void Start () {
		PathNode = GetComponentInChildren<Node> ();
		// this will get all the children with the node script

		//we will use the foreach Loop to check it
		foreach (Node n in PathNode) {
			Debug.Log (n.name);
		}
	}
	*/
	
	// Update is called once per frame
	void Update () {
		
	}
}




























